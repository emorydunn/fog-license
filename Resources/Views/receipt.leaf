<html>
  <head>
    <link rel="stylesheet" href="/css/bulma.min.css" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Thank you for your purchase</title>
  </head>

  <body>
    <p>Receipt from Lost Cause Photographic, LLC</p>

    <p>Order Summary</p>
    <table>
      #for(item in items):
      <tr>
        <td>
          #(item.name)
          <br />
          #if(item.includesUpdates):
          <p>Includes updates</p>
          #endif
        </td>
        <td>#(item.price)</td>
      </tr>
      #endfor
    </table>

    #if(showProcessingMessage):
    <p>
      Your order is still processing, you'll receive an email when it's
      complete.
    </p>
    #endif #if(receiptUrl):
    <p>View your <a href="#(receiptUrl)">full receipt</a>.</p>
    #endif
  </body>
</html>
